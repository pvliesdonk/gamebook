The vault lookup field — serif-set with a leading lens glyph. Use `lg` for the front-page search, `md` in headers.

```jsx
<SearchInput size="lg" placeholder="Search 240 entries…" onChange={e => setQ(e.target.value)} />
```
