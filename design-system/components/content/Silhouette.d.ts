import * as React from 'react';

/** The italic one-line definition that opens every entry — the idea recognised at a glance. */
export interface SilhouetteProps {
  children: React.ReactNode;
}
export function Silhouette(props: SilhouetteProps): JSX.Element;
