The guide's honesty mechanism — a mono bracket-tag declaring where a claim comes from. Place inline at the end of a sentence or beside an entry title.

```jsx
Every clue must be falsifiable. <ProvenanceMark kind="synthesis" />
```

`kind`: `corpus` (drawn from the vault), `researched` (external source), `synthesis` (author's argument).
